<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- Use this for emulator -->
        <domain includeSubdomains="true">127.0.0.1</domain> <!-- Use this for emulator -->
        <domain includeSubdomains="true">192.168.1.100</domain> <!-- Use this for real device -->
        <domain includeSubdomains="true">192.168.1.35</domain> <!-- Use this for real device -->
        <domain includeSubdomains="true">localhost</domain> <!-- Use this for real device -->
        <domain includeSubdomains="true">82.112.237.3</domain> <!-- Use this for real device -->
    </domain-config>
</network-security-config>